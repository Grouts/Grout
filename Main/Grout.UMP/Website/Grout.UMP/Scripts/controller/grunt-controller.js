angular.module('HPDP')
    .controller('DashboardController',
    ['$scope', '$rootScope', '$filter', '$window', 'WebApi', '$routeParams',
        '$uibModal', '$q', 'config', '$location',
        function ($scope, $rootScope, $filter, $window, metricService, UserInfo, WebApi, $routeParams,
            $uibModal, $q, enumModule, vaInvokeService, config, Utility, $location, AgileUiApi, myWizardService, masService) {

        }
    ]);